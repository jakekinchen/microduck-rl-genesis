# Brief 049 - M6 local provenance archaeology

**Date:** 2026-09-04

## Objective

Audit the one partial and five missing file-level provenance entries through
read-only Git/object history and anonymous immutable public remotes. Upgrade an
entry only when an exact source/blob/commit chain and existing file-level
license support it; otherwise retain a digest-bound terminal blocker.

## Scope

- Trace `microduck/assets/microduck/ball.xml` through its Genesis introduction,
  the official source history, and the later `109e06d` divergence.
- Audit reachable history and public branches, tags, releases, and action
  artifacts for the two demo files and three original ONNX policies.
- Treat repository prose, filenames, generic commands, media appearance,
  embedded metadata, and ONNX self-description as non-authoritative.
- Add an immutable receipt, deterministic validation, mutation probes, and an
  exact regenerated file-provenance inventory.

## Acceptance

- `ball.xml` may become complete only if its exact bytes exist at a public
  official commit and its later delta plus license scope are reconciled.
- Media/policy entries remain missing unless checkpoint/run/exporter/
  normalizer/evidence/source/license material is digest-bound to each file.
- Full authority-enabled tests, receipt hashes, branch hygiene, workflow audit,
  diff checks, and an independent Reviewer decision pass.

## Stop Conditions

- Never import, parse, load, execute, evaluate, approve, or activate an ONNX.
- Do not train, publish, provision compute, use credentials, or infer authority
  from a self-claim or mutable surface.
- Stop at the next genuine external-input or authority boundary and leave Brev
  empty.
