# Slice Brief 041 - M5 fifth CUDA pilot

**Date:** 2026-09-04

## Objective

Consume Manager authority 011 exactly once to execute the accepted fifth CUDA
pilot, recover a self-checking terminal receipt, delete the exact workspace,
confirm empty authenticated inventory, and obtain independent review.

## Authority And Hard Stops

- Manager authority 011 must be committed before the create anchor.
- Execute only `experiments/m5/fifth-pilot-proposal-v1.json` at its accepted
  hashes and only the exact `massedcompute_A100_sxm4_80G_DGX` row.
- One workspace, no fallback or retry, 4,800-second harness timeout, 7,200
  seconds create-to-delete, and `$3.312` maximum.
- Suite first; only after it passes may the four frozen 64-by-5 public-
  development smokes and named normalized ONNX exports run.
- Never run candidate or held-out seeds, the full CUDA matrix, publication,
  activation, transfer, or physical operation.

## Required Evidence

- Exact pre-create hashes, deterministic bundle reproduction, empty inventory,
  catalog row, and dry run.
- Exact workspace name and ID, timestamps, health/readiness observations,
  remote input hashes, one harness invocation, terminal stage, and conservative
  elapsed/cost accounting.
- Complete remote receipt and sorted manifest when a shell exists, exact local
  recovery and independent manifest equality, or an explicit honest no-shell
  boundary.
- Exact-ID deletion and authenticated empty inventory.
- Committed Executor log and independent Reviewer decision before any next
  slice.

## Evidence Boundary

At most this pilot can prove CUDA pipeline compatibility for the four public-
development smokes. It cannot prove M5 completion, accepted task or policy
success, candidate or held-out performance, publication, activation, transfer,
or physical authority.
