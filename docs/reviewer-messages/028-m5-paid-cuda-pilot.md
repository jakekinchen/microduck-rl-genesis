# Reviewer Message 028 - M5 paid CUDA pilot

**Date:** 2026-09-03

## Decision

**GO** - close slice 028 as **terminal-negative only**.

The bounded pilot is accepted as durable negative CUDA-compatibility evidence:
the frozen Linux source/runtime combination cannot enter training. This is not
a successful smoke, does not satisfy M5, and does not authorize another paid
pilot or the full CUDA matrix. Full CUDA execution remains **NO-GO** until a
separate local reconciliation and refreeze receives independent review.

## Evidence Reviewed

- `main` was clean at
  `ab93c90fa54fa7d8adb2563d393f41672f2d1b97`, 74 commits ahead of
  `origin/main`, before this Reviewer change.
- Receipt root
  `receipts/m5/pilot/20260903T2334Z-tudexszf6/` contains 24 tracked files:
  `SHA256SUMS` plus all 23 entries named by that manifest. `shasum -a 256 -c`
  passed every entry, an independently regenerated byte-sorted manifest had no
  diff, and the manifest digest is
  `1e8d4948294b4a4c95a8a73c9e0a7c9ab0fcef4c354a2c11ac29f1eb4ce8566b`.
- The separately tracked sibling
  `receipts/m5/pilot/20260903T2334Z-tudexszf6-manager-reconciliation.json`
  binds the same workspace, run root, rate, and remote manifest digest. It
  records authenticated empty inventory at `2026-09-03T23:51:38Z` and retains
  the explicit provider-invoice uncertainty instead of claiming an exact
  billed amount.
- The receipt binds Genesis contract `bb2a235`, Genesis training `93cd5f2`,
  official walking `109e06d`, official backflip `8bde27e`, and BAM `62bd8ce`.
  The bound Genesis `requirements.txt` pins `genesis-world==1.2.2`; both bound
  Genesis commits access `RigidSolver.dyn_state`.
- Runtime evidence records NVIDIA driver `570.195.03`, CUDA 12.8, Torch
  `2.9.1+cu128` with an A100 80 GB PCIe device, Genesis 1.2.2, and the separate
  official MJLab 1.3.0 / MuJoCo 3.10.0 / Warp 1.12.0 lane.
- `logs/full-suite.log` directly records repeated
  `AttributeError: 'RigidSolver' object has no attribute 'dyn_state'` failures
  in the BAM closed loop, external torque, BAM-versus-MuJoCo, domain
  randomization, observation, and flat-environment paths. It also records
  independent model-manifest and evaluator-byte assertion failures plus an
  unselected MuJoCo OpenGL platform. The suite did not complete.
- The manifest-bound harness uses `set -euo pipefail` and places the full suite
  before every training command. The receipt contains no Genesis or official
  smoke log, checkpoint, ONNX export, or other training artifact. Focused M5
  validation regenerated all 32 rows as `planned_not_executed`, with candidate
  or held-out execution unauthorized and no held-out seeds included.
- Cost arithmetic reproduces exactly at the frozen `$1.62/hour` rate:
  1,527 seconds from host boot to terminal stop is `$0.687150`; 1,676 seconds
  from the conservative pre-create anchor to stop is `$0.754200`; 1,708 seconds
  from boot to observed empty inventory is `$0.768600`; and the most
  conservative 1,857-second pre-create-to-empty bound is `$0.835650`. These are
  elapsed-time bounds, not a provider invoice, and remain below `$3.24`.
- A fresh authenticated `brev ls --json` during independent review returned
  `{"workspaces": null}`. No resource was provisioned or modified during
  review.

## Independent Validation

- `bash -n scripts/run_m5_cuda_pilot.sh` - pass.
- `shellcheck scripts/run_m5_cuda_pilot.sh` - pass.
- `.venv-apple/bin/python scripts/validate_m5_experiment.py --matrix-output
  /tmp/microduck-m5-review-028-matrix.json` - pass; 32 unexecuted rows.
- `GS_ENABLE_ZEROCOPY=1 .venv-apple/bin/python tests/run_all.py` - pass, with
  five explicit BAM-checkout skips in the local environment.
- `bash scripts/audit_autonomous_workflow.sh` - clean.

## Non-Blocking Workflow Finding

- `75` - `scripts/check_branch_hygiene.sh bb2a235` returns 2 because its
  immutable-log exemption and manifest scan are hard-coded to
  `receipts/apple-baseline/`, while the byte-preserved M5 `nvidia-smi` and
  supervisor logs contain authentic trailing whitespace. Independent manifest,
  path-completeness, tracked-file, and worktree checks all pass, so this is a
  hygiene-helper scope defect rather than receipt corruption. Do not normalize
  or rewrite the accepted receipt. A future local workflow slice should extend
  the manifest-bound immutable-log policy generically before accepting another
  non-Apple receipt.

## Preserved Gates

- No pilot smoke training started; no candidate or held-out seed was executed.
- No checkpoint, normalized ONNX candidate, evaluator result, policy
  designation, publication, activation, transfer, or physical evidence exists
  from this pilot.
- `official_policy_authority_missing` remains unresolved.
- M5 remains open. Another paid pilot and the 104-GPU-hour/full CUDA envelope
  are not authorized by this decision.
- Any next route must first reconcile and refreeze the Linux Genesis runtime
  against the bound source locally, repair the receipt-hygiene helper without
  altering retained bytes, and obtain a new independent review before compute.

## Terminal Boundary

Slice 028 is closed because it produced a complete, tracked, checksum-bound,
honestly cost-bounded terminal-negative receipt and verified resource teardown.
It is closed as a failed compatibility pilot, not promoted into successful CUDA
pipeline, task-success, held-out, artifact, deployment, transfer, or physical
authority.
