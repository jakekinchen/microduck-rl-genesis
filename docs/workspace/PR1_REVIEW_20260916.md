# PR #1 merge review — September 16, 2026

Scope: [methodology-review foundations](https://github.com/jakekinchen/microduck-rl-genesis/pull/1),
base `942430fb3aeddb9300f07ee99d71010db9ea6295`, initially reviewed head
`761cc8e9010e901a554b98f0e40a6a295262c567`. The user explicitly requested
review, merge and a readable continuation prompt. Review and fixes occurred
in the same task; no separate agent or role cycle was used.

## Findings corrected before merge

1. **Incomplete impact traces could yield a plausible comparison.** Matching
   the final time and common-grid rows did not catch deleted leading/interior
   samples, duplicated rows, or nonfinite finer-rate samples outside the common
   grid. Results now declare their start/end; comparison validates the declared
   rate, full count, each timestamp, and finite consistent position vectors
   before comparing. Regression cases cover both rates and malformed clocks.
2. **Unspecified comparison identities could be treated as matching.** Six
   completed runs with all four identity fields set to null or empty strings
   passed aggregation. Each identifier must now be a lowercase SHA-256, each
   run seed an integer, and the arm list exactly two distinct names. This checks
   schema/equality only; callers still must authenticate their source receipts.
3. **Current documentation contradicted the accepted direction.** READMEs and
   the active brief still said the review was pending; other text said a merge
   was unauthorized or all native experiments must finish before leaving draft.
   Current documents now distinguish authorized foundation-code merge from the
   native acceptance gates that remain in the ordered queue. The continuation
   prompt starts with a bounded native decision instead of another broad review.

## Verification

- `./scripts/duck verify`: 141 passed; no failures, errors or skips.
- `.venv-apple/bin/python -m unittest discover -s tests/retool -v`:
  46 passed after review fixes; no failures, errors or skips. This includes
  synthetic update/parameter/RNG equivalence against the retained PPO method
  under RSL-RL 5.4.2 and the actual retained perception/follower interface.
- `duck doctor`: all four availability/contract/BAM checks passed.
  `retool doctor`: required packages/paths present; runtime remains unvalidated.
- Archived pre-retool GOAL and queue match base-commit bytes exactly. Historical
  V1–V66 implementations and receipts are outside the change.
- On initial reviewed head `761cc8e`, full-checkout pinned CPU CI ran 42/42
  candidate tests without skips:
  [run 35138408961](https://github.com/jakekinchen/microduck-rl-genesis/actions/runs/35138408961).
  Contract and workspace checks also passed. The final corrected head must
  separately pass all three automatic workflows before merge; GitHub checks on
  [the PR](https://github.com/jakekinchen/microduck-rl-genesis/pull/1/checks)
  provide that commit-specific result.

The original partial-checkout validation JSON and CI failure explanation remain
historical records. They are not overwritten by the full-checkout results.
The local status projection reports 24 known symlink-inspection refusals for
external V54/V55 logs; its read-only viewer intentionally does not follow them.
Neither doctor nor those refusals supply new behavior evidence.

## Merge decision and remaining limits

No unresolved blocking issue was found within the opt-in foundation scope after
the fixes. Merge is appropriate once the corrected head's automatic checks pass.
These helpers have no automatic launch or admission path and do not replace any
retained actor. The native exact-clone experiment, Rust transport, full RGB bank,
feasibility search, native training integration and new learned results are
still pending. No simulator, training, paid compute or hardware was started for
this review. Software verification does not establish physical calibration or
general carpet walking.

The next deliverable is described in [the continuation prompt](NEXT_AGENT_PROMPT.md)
and stage 1 of [the ordered queue](../../TRAINING_ACTUALIZATION.md).
